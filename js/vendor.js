// vendor.js

// ========== jQuery (example) ==========
// (function(){ /* jQuery code if you want to include it directly */ })();

// ========== Slick Slider ==========
// (function(){ /* Slick plugin code if needed for a testimonials slider */ })();
